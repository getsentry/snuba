syntax = "proto3";

import "snuba/protobufs/Filters.proto";

message FindTraceRequest {
  message Filter {
    oneof value {
      SpanFilter span_filter = 1;
    }
  }
  repeated Filter filters = 1;
}

message FindTraceResponse {
  repeated string trace_uuids = 1;
}
